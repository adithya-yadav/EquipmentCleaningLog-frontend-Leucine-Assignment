# Equipment Cleaning Log — Frontend

React + TypeScript + Vite frontend for the Equipment Cleaning Log assignment.

## Setup

```bash
npm install
```

Optional: copy `.env.example` to `.env` if the API URL is different.

Start:

```bash
npm run dev
```

The app normally runs at http://localhost:5173.

Make sure the backend is running at http://localhost:5000.
